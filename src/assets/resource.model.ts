export interface Resource {
    cover: string; // URL for the cover image
    title: string;
    author: string;
    isbn: string;
    year: number | null;
    publication: string;
    description: string;
  }
  